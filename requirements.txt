# Python 3.11.  Pinned to what the results were produced with.
#
# --- Query the pre-built graph (this is all the one-command demo needs;
#     no API key required when using --no-llm) ---
numpy==1.26.4
openai==2.45.0            # Together API is OpenAI-compatible; only needed for LLM prose/labeling
python-dotenv==1.2.2

# --- Full pipeline: build a graph from PDFs (extraction, embedding, DeBERTa pairing) ---
networkx==3.4.2          # graph inspection (load_graph.py)
pypdf==6.14.2            # PDF -> text/chunks
rapidfuzz==3.14.5        # span/quote matching + dedup
sentence-transformers==5.6.0   # DeBERTa NLI (pairing --nli) + dedup embeddings
torch==2.2.2             # backend for sentence-transformers
